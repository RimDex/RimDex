# RimDex `.gitignore` Exclusion Map

> Moved from `AGENTS.md` §6 (2026-09-23) so the agent instruction file stays
> focused on status and guardrails. This is the canonical exclusion map for the
> repo: treat the paths listed here as **excluded** (not counted in LOC, not
> committed, not scanned). "Present" = actually exists on disk and is excluded
> right now.

## Top-level directory map

| Path (top level)            | Status vs root `.gitignore` | Notes |
|-----------------------------|-----------------------------|-------|
| `app/`                      | included                    | tracked source |
| `tests/`                    | included                    | tracked source |
| `docs/`                     | included                    | tracked source |
| `libs/`                     | included                    | tracked source |
| `locales/`                  | included                    | `locales/.translation_cache.json` excluded |
| `packaging/`                | included                    | tracked source |
| `scripts/`                  | included                    | tracked source — hosts `stats.py`, `check.py`, `mapping.py` |
| `submodules/`               | included                    | tracked source (git submodules) |
| `themes/`                   | included                    | tracked source |
| `.github/` `.githooks/`     | included                    | tracked config |
| `.continue/`                | **excluded**                | root `.gitignore` IDE section |
| `.vscode/` `.ropeproject/`  | **excluded**                | `.vscode/*` + `.ropeproject/`; `launch.json` + `settings.json` stay tracked |
| `__pycache__/`              | **excluded**                | `*.py[cod]` bytecode cache |
| `.pytest_cache/`            | **excluded**                | pytest cache |
| `.mypy_cache/`              | **excluded**                | mypy cache |
| `.ruff_cache/`              | **excluded** (root rule)    | promoted to root `.gitignore` |
| `.venv/`                    | **excluded**                | virtualenv |
| `.tools/`                   | **excluded**                | dev tooling — not present right now |
| `.cocoindex_code/`          | **excluded**                | CocoIndex — not present right now |
| `build/`                    | **excluded**                | Nuitka build output — present |
| `todds/`                    | **excluded**                | `/todds` — present |
| `MagicMock/`                | **excluded**                | test artifact leak — present |
| `.translation_cache.json`   | **excluded**                | translation cache (root) — none present |
| `steamworks_sdk_*.zip`      | **excluded**                | — none present |

## Per-rule breakdown

Each root `.gitignore` rule → the repo locations it maps to. "Present" = seen in
the working tree; "(none yet)" = rule is armed but currently no matching file.

| `.gitignore` rule            | Maps to (in this repo) | Present? |
|------------------------------|------------------------|----------|
| `__pycache__/`               | every Python package: root + `app/**` (32 sub-packages), `tests/**` (18), `submodules/SteamworksPy/steamworks/` | 46 dirs |
| `*.py[cod]`                  | loose bytecode (normally inside `__pycache__/`) | (none loose) |
| `*$py.class`                 | Jython class files | (none) |
| `build/`                     | Nuitka onefile output | yes |
| `dist/`                      | distribution dir | (none) |
| `*.egg-info/` `*.egg`        | setuptools artifacts | (none) |
| `MANIFEST`                   | sdist manifest | (none) |
| `.pytest_cache/`             | pytest cache | yes |
| `.coverage` `.coverage.*`    | coverage data files | `.coverage` (post-`test-coverage`) |
| `htmlcov/`                   | coverage HTML report | yes (post-`test-coverage`) |
| `coverage.xml`               | coverage XML | yes (post-`test-coverage`) |
| `nosetests.xml`              | nose report | (none) |
| `junit/`                     | JUnit XML dir | yes (post-`test-coverage`) |
| `.mypy_cache/`               | mypy cache | yes (140 dirs) |
| `.dmypy.json` `dmypy.json`   | dmypy state | (none) |
| `.env` `.venv` `env/` `venv/` `ENV/` | env files / venvs | `.venv/` only |
| `.idea/` `.vs`               | IDE dirs | (none in tree) |
| `*.build/` `*.dist/` `*.onefile-build/` | Nuitka artifact dirs | (none) |
| `nuitka-crash-report.xml`    | Nuitka crash report | (none) |
| `DEBUG`                      | debug log/dir | (none) |
| `.DS_Store`                  | macOS cruft | (none) |
| `/todds`                     | `todds/` | yes |
| `steamworks_sdk_*.zip`       | `steamworks_sdk_*.zip` | none present |
| `version.xml`                | generated by GH Actions | (none yet) |
| `ltex.dictionary*`           | LanguageTool dict | (none) |
| `.translation_cache.json`    | root + `locales/` | none present |
| `MagicMock/`                 | test artifact leak (`mock.aux_db_path/*`) | yes |
| `.tools/`                    | dev tooling | none present |
| `/.cocoindex_code/`          | CocoIndex | none present |

## Exclusions NOT covered by the root `.gitignore` (flagged)

All previously flagged items are resolved: `.ruff_cache/` promoted to the root
`.gitignore`, the stale `app/views/mod_list_widget_mixins/` directory removed,
and `.vscode/*` / `.ropeproject/` / `.continue/` added to the root IDE section
(the two tracked `.vscode/*.json` files stay versioned).
